@php
    $breadcrumb = getContent('breadcrumb.content',true);
@endphp

<!-- Hero Section -->
<section class="hero-section bg--title shapes-container">
    <div class="container">
        <h2 class="hero-title">{{ __($page_title) }}</h2>
    </div>
</section>
<!-- Hero Section -->
